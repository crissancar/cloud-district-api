export enum ClubDomainEvents {
	PLAYER_SIGNED = 'club.player.signed',
	PLAYER_RELEASED = 'club.player.released',
	MANAGER_SIGNED = 'club.manager.signed',
	MANAGER_RELEASED = 'club.manager.released',
}
